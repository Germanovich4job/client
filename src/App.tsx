import "./App.css"
import ProductList from "./components/ProductList"

export const App = () => (
  <div className="App">
    <header className="App-header">
      <ProductList />
    </header>
  </div>
)
